const {t}=require('../../utils/i18n');const db=require('../../utils/db');Page({data:{i18n:{},exercises:[],categories:['All','Chest','Back','Shoulders','Arms','Core','Legs'],activeCategory:'All',favorites:[],loading:true,allExercises:[]},onLoad(){this.updateI18n();this.loadExercises();this.loadFavorites();},updateI18n(){const app=getApp();const lang=app.globalData.userConfig.language||'en';this.setData({i18n:{searchPlaceholder:t('searchPlaceholder',lang),noResults:t('noResults',lang)},currentLang:lang});},loadExercises(){try{const mockData=require('../../data/exercises.json');this.setData({exercises:mockData.slice(0,50),allExercises:mockData,loading:false});}catch(e){this.setData({loading:false});}},loadFavorites(){const fav=db.getFavorites();this.setData({favorites:fav.map(f=>f.id)});},selectCategory(e){const category=e.currentTarget.dataset.cat;this.setData({activeCategory:category});const filtered=category==='All'?this.data.allExercises:this.data.allExercises.filter(ex=>ex.body_part&&ex.body_part.toLowerCase().includes(category.toLowerCase()));this.setData({exercises:filtered.slice(0,50)});},toggleFav(e){const id=e.currentTarget.dataset.id;const exercise=this.data.exercises.find(ex=>ex.id===id);if(db.isFavorite(id)){db.removeFavorite(id);}else{db.addFavorite(exercise);}this.loadFavorites();},isFav(id){return this.data.favorites.includes(id);},goDetail(e){const id=e.currentTarget.dataset.id;wx.navigateTo({url:`/pages/exercise-detail/exercise-detail?id=${id}`});},loadMore(){const current=this.data.exercises.length;const more=this.data.allExercises.slice(current,current+20);if(more.length>0){this.setData({exercises:this.data.exercises.concat(more)});}},onShow(){this.updateI18n();this.loadFavorites();}});